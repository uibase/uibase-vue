<% for (var i = 0; i < paths.length; i++) { %>
import Base<%- paths[i][0] %> from '<%- paths[i][1] %>'
<% } %>

export default {
    install(Vue, options) {
        <% for (var i = 0; i < paths.length; i++) { %>
        Vue.component('Base<%- paths[i][0] %>', Base<%- paths[i][0] %>)
        <% } %>
    }
}

